# PyTorch CPU-only build — must come before the packages so pip resolves it
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchvision
torchaudio

# HuggingFace ecosystem
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.29.0
safetensors>=0.4.0

# LoRA adapter + supervised fine-tuning trainer
peft>=0.10.0
trl>=0.8.6

# GGUF read/write (Ollama model blob conversion)
gguf>=0.6.0

# Gemma tokenizer backend
sentencepiece>=0.1.99

# Memory usage telemetry (optional — callback falls back gracefully without it)
psutil>=5.9.0

# Progress + display
tqdm>=4.66.0
rich>=13.0.0
